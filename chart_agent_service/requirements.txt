fastapi>=0.104.0
uvicorn>=0.24.0
yfinance>=0.2.31
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
httpx>=0.25.0
python-dotenv>=1.0.0
apscheduler>=3.10.0
feedparser>=6.0.0

# ── 고급 ML (앙상블 + SHAP 설명력) ──
shap>=0.44.0
lightgbm>=4.1.0
xgboost>=2.0.0
tensorflow[and-cuda]>=2.15.0  # [and-cuda]: nvidia-cudnn/cublas/runtime 등 자동 설치 (GPU 가속용)

# ── HyperOpt (Freqtrade 스타일) ──
optuna>=3.5.0

# ── Settings 검증 (Phase 2b) ──
pydantic-settings>=2.0
