fastapi>=0.104.0
uvicorn>=0.24.0
yfinance>=0.2.31
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
httpx>=0.25.0
orjson>=3.10.0
python-dotenv>=1.0.0
apscheduler>=3.10.0
feedparser>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.11.0

# ── 고급 ML (앙상블 + SHAP 설명력) ──
shap>=0.44.0
lightgbm>=4.1.0
xgboost>=2.0.0
tensorflow[and-cuda]>=2.15.0,<2.21  # [and-cuda]: nvidia-cudnn/cublas/runtime 등 자동 설치 (GPU 가속용)

# ── HyperOpt (Freqtrade 스타일) ──
optuna>=3.5.0

# ── Settings 검증 (Phase 2b) ──
pydantic-settings>=2.0

# ── LLM Router / 재시도 / 회로차단 ──
litellm>=1.83.0
tenacity>=9.0.0
circuitbreaker>=2.1.0

# ── 거래일 캘린더 (Step 11) ──
pandas_market_calendars>=4.4.0

# ── DART 공시 (P2) ──
# CLAUDE.md §8에 P2 의존성으로 명시돼 있었지만 실제 선언이 누락돼
# 컨테이너에서 dart_client가 ModuleNotFoundError로 조용히 폴백했다 (2026-07-30).
# 상한 고정: 0.2.3은 Requires-Python >=3.13 — 본 프로젝트는 3.12 고정(§5.1).
# 3.13 전환 시 이 핀을 함께 풀 것.
opendartreader>=0.2.2,<0.2.3
