"""WebUI 공용 모듈.

`webui.py` 는 6,482 라인 단일 파일이었다. CLAUDE.md §6-10 대로 **한 번에 쪼개지 않고**
의존성이 낮은 순서로 뗀다:

  1. `theme.py`      — CSS 토큰 (순수 문자열, 의존성 0)
  2. `api_client.py` — agent-api 호출 (모든 페이지가 쓰는 공용 경로)
  3. 이후 페이지 단위 (`pages/`) — 상태 결합이 있어 한 번에 하나씩

각 단계마다 브라우저로 실제 렌더를 확인한다 — Streamlit 은 스크립트가 죽어도
HTTP 200 을 준다 (CLAUDE.md §13-5).
"""
