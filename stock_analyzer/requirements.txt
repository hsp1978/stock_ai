# stock_analyzer — WebUI + local_engine bridge
# chart_agent_service/ 의존성도 함께 필요 (local_engine에서 직접 import)

# ── WebUI ──
streamlit>=1.30.0
plotly>=5.18.0
yfinance>=0.2.31
finance-datareader>=0.9.0  # Korean stock investor trading data (fallback)
pykrx>=1.0.0  # Korean stock market data
beautifulsoup4>=4.11.0  # HTML parsing for Naver Finance

# ── 데이터/수학 ──
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# ── ML (chart_agent_service/ml_predictor) ──
scikit-learn>=1.3.0

# ── 차트 생성 (chart_agent_service/analysis_tools) ──
matplotlib>=3.7.0

# ── HTTP / 설정 ──
httpx>=0.25.0
requests>=2.31.0
pytz>=2024.1
python-dotenv>=1.0.0

# ── LLM ──
google-generativeai>=0.3.0
openai>=1.0.0

# ── 스케줄러 (scanner.py) ──
apscheduler>=3.10.0
pydantic-settings>=2.0
